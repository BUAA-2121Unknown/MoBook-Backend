name: mobook

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      PROJ_DIR: ${{ PROJECT_DIR }}
      PRODUCTION_PORT: ${{ PORT }}
    steps:
      - name: setup SSH
        run: sudo apt install sshpass

      - name: init project
        run: >
          sshpass -p ${{ secrets.PASSWORD }} ssh -o StrictHostKeyChecking=no ${{ secrets.USER }}@${{ secrets.SSH_IP }}
          "mkdir -p ${{ PROJ_DIR }}; cd ${{ PROJ_DIR }}; git config --global http.postBuffer 1048576000; git pull"

      - name: establish connection
        run: >
          sshpass -p ${{ secrets.PASSWORD }} ssh -o StrictHostKeyChecking=no ${{ secrets.USER }}@${{ secrets.SSH_IP }}
          "cd ${ PROJ_DIR }; killall -9 daphne; daphne -b 0.0.0.0 -p ${{ PRODUCTION_PORT }} MoBook.asgi:application > daphne.log 2>&1 &"
